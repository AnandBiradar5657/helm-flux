apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: backend
  #namespace: default
spec:
  interval: 1m
  chart:
    spec:
      chart: podinfo
      version: ">=4.0.0 <5.0.0"
      sourceRef:
        kind: HelmRepository
        name: demo-chart
        namespace: helm
      interval: 1m
  values:
    replicaCount: 2  
    #foo
